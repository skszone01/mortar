# ข้อมูลที่สกัดจากไฟล์เกม Arma Reforger

ไฟล์ในโฟลเดอร์นี้ **ยังไม่ได้ถูกใช้งานในเว็บ** เก็บไว้เป็นข้อมูลอ้างอิง/สำรอง

ที่มา: แกะจาก `.pak` ของเกม (Enfusion PAC1) ที่
`D:\SteamLibrary\steamapps\common\Arma Reforger\addons\data\` (ตารางกระสุนอยู่ใน `data007.pak`)
และจาก MOD Adult Mortars v1.0.4 ที่
`Documents\My Games\ArmaReforger\addons\AdultMortars_6318DCB19B389CC8\data.pak`

---

## `shell_physics_from_game.json`

ค่าที่ใช้ในโหมด "คำนวณขั้นสูง" ของเว็บ (คัดลอกไปอยู่ใน `script.js` แล้วในชื่อ `SHELL_PHYSICS`)

สมการที่เอนจิ้นเกมใช้จริง (จาก `ShellMoveComponent` + สคริปต์ `BallisticTable.c`):

```
a = −g·ŷ − (AirDrag ÷ Mass)·|v−w|·(v−w)        g = 9.81, w = เวกเตอร์ลม
```

ค่าดิบต่อกระสุน (จาก `Prefabs/Weapons/Ammo/Ammo_Shell_*.et`):

| กระสุน | InitSpeed | Mass | AirDrag | k = AirDrag/Mass |
|---|---|---|---|---|
| M821 (81mm HE) | 66 | 4.06 | 0.000462 | 1.1379e-4 |
| M819 (81mm Smoke) | 137 | 4.85 | 0.0009139 | 1.8843e-4 |
| M853A1 (81mm Illum) | 152 | 4.00 | 0.001488 | 3.7200e-4 |
| 0-832Ay / O832DU (82mm HE) | 76 | 3.10 | 0.000615 | 1.9839e-4 |
| A-832AY / D832DU (82mm Smoke) | 71 | 3.48 | 0.000655 | 1.8822e-4 |
| C-832C / S832S (82mm Illum) | 127 | 3.51 | 0.001836 | 5.2308e-4 |

- ความเร็วต้นของแต่ละ ring ในไฟล์ JSON ปรับเทียบกับตารางที่เกม/mod แจกมาแล้ว **คลาดเคลื่อน ≤ 0.05%**
- MOD Adult Mortars ใช้กระสุนตัวเดียวกัน (ค่า drag เท่ากัน) เปลี่ยนแค่แรงส่งของแต่ละ ring
- ฐานปืนจำกัดมุมยกไว้ที่ 45°–85° (`Mortar_Base.et`: `LimitsVert 45 85`)
- **ไม่มี** โมเดลความกดอากาศ/ความหนาแน่นอากาศในเกม — มีแค่ค่า `AirDrag` ค่าเดียวต่อกระสุน
- M107 ไม่มีอยู่ในเกมจริง (ไม่พบใน pak ใด ๆ)

## `wind_data_vanilla_from_game.js`

> ✅ **นำไปใช้ในเว็บแล้ว** — `WIND_DATA` ใน `script.js` ตอนนี้คือชุดนี้ ไฟล์นี้เก็บไว้เป็นต้นฉบับสำรอง

ตารางค่าแก้ลมของกระสุน Original ที่สกัดจากไฟล์เกมโดยตรง
(`WindData_Shell_*.conf` ใน `data007.pak`) — รูปแบบเดียวกับ `WIND_DATA` ใน `script.js`
ใช้จุดระยะชุดเดียวกัน

> ⚠️ **ไฟล์นี้ล้าสมัยแล้ว** — มันถูกสร้างด้วยสคริปต์ที่จับคู่แถวผิด (ดูหัวข้อถัดไป)
> ค่าที่ใช้จริงใน `script.js` ตอนนี้เป็นชุดที่สร้างใหม่แล้ว เก็บไฟล์นี้ไว้เป็นบันทึกประวัติเท่านั้น

**เหตุผลที่แทนที่ของเดิม:** `WIND_DATA` ชุดเก่าคัดลอกมาจาก arma-mortar.com
ซึ่งคอลัมน์ Zw (ค่าแก้ทิศจากลมขวาง) สูงเกินจริงอย่างเป็นระบบ

| | คลาดเคลื่อนกลาง |
|---|---|
| Xw (ค่าแก้ระยะ) | 4.2% — ใช้ได้ |
| Zw (ค่าแก้ทิศ) | 22% — เพี้ยนมาก |

ยิ่ง ring สูงยิ่งเพี้ยน (ring 0 ≈ 1%, ring 4 ≈ 33%) และกระสุนส่องสว่างหนักสุด
เช่น C-832C ring 4 ที่ 500 m: เว็บ **408** แต่เกมจริง **174**

ส่วน `WIND_DATA_MOD` ตรวจซ้ำแล้ว **ถูกต้องทุกค่า** ไม่ต้องแก้

---

## บันทึก: ตารางลมถูกแก้ 2 รอบ

**รอบที่ 1** — เปลี่ยนจากชุดที่ลอกมาจาก arma-mortar.com (Zw สูงเกินจริง) มาเป็นชุดที่สกัดจากไฟล์เกม

**รอบที่ 2** — พบว่าสคริปต์ที่ใช้ในรอบแรกจับคู่แถวผิด จึงสร้างใหม่ทั้งหมด

ในไฟล์ `WindData_Shell_*.conf` แต่ละประจุมีอาร์เรย์ 90 แถวสองชุด:
- `m_aFiringSolution[i]` = `[มุมยิง (เรเดียน), ระยะ (m), ความสูงสูงสุด (m)]`
- `m_aValues[i]` = `[ค่าแก้ทิศ (มิลลิเรเดียน), ค่าแก้ระยะ (m), **มุมตกกระทบ** (องศา)]`

สคริปต์รอบแรกไปไล่หาแถวใน `m_aValues` ที่คอลัมน์ *มุมตกกระทบ* ตรงกับ *มุมยิง* — ซึ่งผิด
เพราะมุมตกกระทบมากกว่ามุมยิงเสมอ (แรงต้านอากาศทำให้ขาลงชันกว่าขาขึ้น) จึงหยิบแถวที่ตื้นกว่ามาใช้

ซอร์สโค้ดเกม `SCR_ProjectileWindData.c` ยืนยันว่าต้องจับคู่ **ตามลำดับแถว**:

```c
vector fireSolutionOut = vector.Lerp(first, second, t);
vector valuesOut = vector.Lerp(m_aValues[midId-1], m_aValues[midId], t);
```

ใช้ `midId` และ `t` ตัวเดียวกันทั้งสองอาร์เรย์ คอลัมน์มุมตกกระทบเป็นแค่ข้อมูลผลลัพธ์ ไม่ใช่ดัชนี

ผลของการแก้: Zw เพิ่มขึ้น 3–94% (ยิ่ง ring สูงยิ่งเพิ่มมาก เพราะช่องว่างระหว่างมุมยิงกับมุมตกกว้างขึ้น)
กระสุนส่องสว่างที่แรงต้านอากาศสูงเพี้ยนหนักสุด — C-832C ring 4 ที่ 400 m: เดิม 217 → ใหม่ **440**
อีกจุดคือลืมแปลงหน่วยมิลลิเรเดียนเป็นมิล (NATO ×1.0186, รัสเซีย ×0.9549)

ชุดใหม่ตรวจสอบด้วยการจำลองวิถีกระสุนอิสระ 19 จุด ได้อัตราส่วนกลาง **1.000** ทั้ง Zw และ Xw

---

## หมายเหตุ: ค่าคูณประจุของ A-832AY ใน MOD มี 2 ชุดที่ไม่ตรงกัน

- `Ammo_Shell_82mm_Smoke_D832DU.et` (ตัวกระสุนที่เกมยิงจริง) ใช้ **2.423, 2.975**
- `AM_M_MortarBallisticTableGeneratorConfig_D832DU.conf` (ตัวสร้างตาราง) ใช้ **2.428, 2.981**

เว็บปรับเทียบตาม *ตัวสร้างตาราง* เพื่อให้ตรงกับตารางที่ผู้เล่นเห็นในเกม
ผลคือคลาดจากกระสุนจริงราว 0.2% ของความเร็ว (≈ 0.4% ของระยะ หรือ ~10 m ที่ 2.7 กม.)
เป็นความไม่ตรงกันภายในตัว mod เอง ไม่ใช่ข้อผิดพลาดของเว็บ

นอกจากนี้ mod นับ ring ของกระสุนนี้เป็น **0–3** (ตาม `m_aChargeRingConfig`) เหมือนกับของ Original
เว็บเคยใส่เป็น 1–4 ซึ่งผิด แก้แล้ว

---

## หมายเหตุสำคัญ: คอลัมน์ Hw ในตารางเกมไม่ใช่ค่าแก้ความสูงที่แท้จริง

จากสคริปต์ `SCR_VisualisedBallisticConfig.c` ของเกม คอลัมน์ Hw ถูกสร้างจาก

```
Hw = | มุมยกที่ระยะ (R + 50) − มุมยกที่ระยะ R |
```

เท่ากับ**สมมติว่ามุมตกกระทบคงที่ 63.4°** (คือแปลงความสูง 100 m เป็นระยะ 50 m เสมอ)
ทั้งที่มุมตกจริงในเกมอยู่ระหว่าง 64°–86° ค่าแก้จึงเพี้ยนมากเมื่อความสูงต่างกันเยอะ

ตัวเกมเองตอนยิงจริง**ไม่ได้ใช้ค่านี้** แต่จำลองวิถีใหม่ทั้งเส้น
(`BallisticTable.GetAimHeightOfProjectileAltitudeFromSource` — คอมเมนต์ในสคริปต์เขียนว่า
"This method simulates the projectile, so it takes considerable time to run!")

นี่คือเหตุผลที่เว็บเพิ่มโหมด "คำนวณขั้นสูง" เข้ามา

---

## หมายเหตุ: ขีดจำกัดมุมยกของ M777 (แก้แล้ว 14 ส.ค. 2569)

`M777_Howitzer.et` ระบุ `LimitsVert 0 71.7` — ขอบล่างคือ **0°** ไม่ใช่ 45°

เว็บเคยตั้ง `ELEV_LIMITS.m777.min = 45` โดยลอกมาจากมอร์ต้าร์ ซึ่งผิด เพราะ
M777 เป็นกระสุนความเร็วสูง มุมที่ยิงได้ไกลสุดอยู่ราว **38–43.5°** ไม่ใช่ 45°
การตัดที่ 45° จึงทำให้เสียระยะไป

| ประจุ | ระยะไกลสุดจริง | มุมที่ได้ระยะนั้น | ที่ 45° (ของเดิม) | เสียไป |
|---|---|---|---|---|
| 3 | 11673 m | 41.0° | 11582 m | 91 m |
| 4 | 15508 m | 39.5° | 15297 m | 211 m |
| 5 | 19049 m | 38.0° | 18664 m | 385 m |

เป้าหมายที่อยู่ในช่วงที่เสียไปเคยถูกรายงานว่า "ไกลเกินไป" ทั้งที่ยิงถึง

การแก้ต้องทำ 3 จุดพร้อมกัน ไม่งั้นไม่มีผล:
1. `ELEV_LIMITS.m777.min: 45 → 0`
2. `BALLISTIC_SIM.solve()` เดิมอ่านขีดจำกัดด้วย `(limits && limits.min) || MIN_ELEV_DEG`
   ซึ่ง `0 || 45` ได้ 45 — ต้องเปลี่ยนเป็น `Number.isFinite(limits.min)`
3. การกวาดหาจุดยิงไกลสุดเดิมทำเฉพาะตอนเป้าอยู่สูงกว่า (`heightDiff > 0`)
   กรณีอื่นสมมติว่ามุมไกลสุด = ขอบล่าง ซึ่งจริงเฉพาะมอร์ต้าร์ (45°) — ตอนนี้กวาดทุกกรณี

ส่วนมอร์ต้าร์ยังเป็น 45–85° ตาม `Mortar_Base.et` (mod สืบทอดค่าเดียวกัน) ค่าที่คำนวณได้ไม่เปลี่ยน

## ยังไม่ได้แก้: ตารางลมของ MOD (`WIND_DATA_MOD`)

การแก้รอบที่ 2 ข้างบน (จับคู่แถวตามลำดับ + แปลงมิลลิเรเดียนเป็นมิล) ถูกนำไปใช้กับ
`WIND_DATA` และ `WIND_DATA_M777` แล้ว แต่ **ยังไม่ได้ทำกับ `WIND_DATA_MOD`**
ค่าแก้ทิศ (Zw) จึงยังต่ำกว่าจริง 8–75% (ยิ่ง ring สูงยิ่งเพี้ยน, ring 0 ปกติ)

ตารางนี้ใช้เฉพาะในส่วน "เทียบกับวิธีตาราง" คำตอบหลักที่ผู้เล่นใช้ยิงมาจากการจำลอง
ซึ่งคิดลมจากสมการโดยตรงอยู่แล้ว จึงไม่กระทบการยิงจริง แต่ทำให้คอลัมน์เทียบดูผิด

---

## เปลี่ยนความเร็วต้นของกระสุน MOD มาใช้ค่าจากไฟล์กระสุนโดยตรง (14 ส.ค. 2569)

เดิม `SHELL_PHYSICS.mod` ปรับเทียบ (fit) กับตารางที่ mod แจกมา ตอนนี้ใช้

```
v0(ring) = InitSpeed ของกระสุน x ตัวคูณใน m_aChargeRingConfig ของ Ammo_Shell_*.et
```

ซึ่งเป็นค่าที่เอนจิ้นเกมส่งกระสุนออกไปจริง

**เหตุผล:** ตารางของ mod เองสั้นกว่าฟิสิกส์จริง 0.18–0.39% (พิสูจน์ได้ว่าเป็นข้อผิดพลาด
ตอนสร้างตาราง ไม่ใช่ฟิสิกส์คนละชุด — เกมต้นฉบับกับ mod แจกตารางสำหรับค่าสัมประสิทธิ์
ชุดเดียวกันและ `ShellMoveComponent` เหมือนกัน แต่ตัวเลขไม่ตรงกัน และไม่มีคู่ (v0,k) ใด
ที่พอดีกับแถวของ mod ได้ — ค่าคลาดเหลือแย่กว่ากลุ่มควบคุมของเกมต้นฉบับ 12 เท่า)

ค่าที่เปลี่ยน (สูงขึ้นทุกตัว 0.10–1.59%):

| กระสุน | ring | เดิม | ใหม่ | ต่าง |
|---|---|---|---|---|
| M821 | 4 | 327.49 | 327.954 | +0.14% |
| M819 | 4 | 344.51 | 345.377 | +0.25% |
| M853A1 | 4 | 692.67 | 700.872 | +1.18% |
| 0-832Ay | 4 | 282.99 | 283.556 | +0.20% |
| A-832AY | 3 | 211.33 | 211.225 | −0.05% |
| C-832C | 4 | 647.66 | 657.987 | +1.59% |

ตัวคูณของ A-832AY ใช้ชุดของไฟล์กระสุน (1.806 / 2.423 / 2.975) ไม่ใช่ชุดของตัวสร้างตาราง
(2.428 / 2.981) ตามหลักเดียวกัน — ดูหัวข้อ "ค่าคูณประจุของ A-832AY ใน MOD มี 2 ชุด" ด้านบน

**ผลที่ตามมา:** มุมยกจากการจำลองสูงกว่าตารางของ mod ราว 1–2 มิล (M853A1 ring 4 ที่ 4200 m
ต่างกัน 5 มิล) ซึ่งเป็นผลที่คาดไว้ เพราะตาราง mod เองสั้น ค่าที่แสดงตอนนี้ตรงกับฟิสิกส์ของเกม
ส่วนตัวเลขในคอลัมน์ "ตาราง" ของหน้าเทียบยังเป็นค่าเดิมของ mod ไม่ได้แก้

**ยังไม่ได้ทำ:** ค่าของเกมต้นฉบับ (`SHELL_PHYSICS.original`) ก็ปรับเทียบด้วยวิธีเดิมเช่นกัน
คลาดจากค่า prefab ราว 0.14% (~4 m ที่ระยะไกลสุด) ยังไม่ได้เปลี่ยนตาม
